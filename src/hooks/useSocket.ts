/*import { useEffect } from "react";
import initSocket from "@/services/serviceSocket";

const useSocket = () => {
  return useEffect(() => {
    const cleanup = initSocket();
    return cleanup;
  }, []);
}

export default useSocket; */